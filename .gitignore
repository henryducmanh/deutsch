# AI task locks (local only, TTL 60min — không đẩy lên remote)
.ai-locks/

# Editor / agent local
.obsidian/
.claude/

# Scrape temp + backup
.tmp_*
*.bak
*backup*.csv

# Python cache
__pycache__/
*.pyc

# Module secrets (api_key, DB password) — chỉ giữ config.example.php
module/**/config.php
